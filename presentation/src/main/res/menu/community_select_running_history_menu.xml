<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/not_ready_to_create_running_post"
        android:visible="false">
        <item
            android:id="@+id/warning_select_running_history_button"
            android:enabled="true"
            android:icon="@drawable/community_create_running_post_uncheck"
            android:title="@string/community_select_running_history"
            app:showAsAction="ifRoom"/>
    </group>

    <group
        android:id="@+id/ready_to_create_running_post"
        android:visible="false">
        <item
            android:id="@+id/running_history_selected_button"
            android:enabled="true"
            android:icon="@drawable/community_create_running_post_check"
            android:title="@string/community_select_running_history"
            app:showAsAction="ifRoom"/>
    </group>
</menu>